---
const { title = "Claudio Marzolo" } = Astro.props;
---

<!doctype html>
<html lang="it">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
  </head>

  <body>
    <header class="site-header">
      <div class="wrap">
        <a class="brand" href="/">Claudio Marzolo</a>

        <nav class="nav">
          <a href="/">Home</a>
          <a href="/biografia">Biografia</a>
          <a href="/biography">Biography (EN)</a>
          <a href="/foto">Foto</a>
          <a href="/pubblicazioni">Pubblicazioni</a>
          <a href="/discografia">Discografia</a>
          <a href="/equipment">Equipment</a>
          <a href="/contatti">Contatti</a>
        </nav>
      </div>
    </header>

    <main class="wrap">
      <slot />
    </main>

    <footer class="site-footer">
      <div class="wrap">
        <small>© {new Date().getFullYear()} Claudio Marzolo</small>
      </div>
    </footer>

    <style>
      :global(body) {
        margin: 0;
        font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
        line-height: 1.5;
      }
      .wrap {
        max-width: 980px;
        margin: 0 auto;
        padding: 0 16px;
      }
      .site-header {
        position: sticky;
        top: 0;
        background: white;
        border-bottom: 1px solid #eee;
      }
      .site-header .wrap {
        display: flex;
        gap: 16px;
        align-items: center;
        justify-content: space-between;
        padding: 14px 16px;
      }
      .brand {
        text-decoration: none;
        color: inherit;
        font-weight: 700;
      }
      .nav {
        display: flex;
        flex-wrap: wrap;
        gap: 10px 14px;
        justify-content: flex-end;
      }
      .nav a {
        text-decoration: none;
        color: #222;
        font-size: 14px;
      }
      .nav a:hover {
        text-decoration: underline;
      }
      main.wrap {
        padding-top: 24px;
        padding-bottom: 40px;
      }
      .site-footer {
        border-top: 1px solid #eee;
        padding: 18px 0;
      }
    </style>
  </body>
</html>